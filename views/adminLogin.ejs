<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>RecruitEase | Admin Login</title>
  <link rel="icon" href="images/logo.jpg" type="image/jpeg"/>

  <style>
    /* Global Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(to bottom right, #f0f2ff, #dce6ff);
      color: #333;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    /* Header Section */
    #header {
      display: flex;
      align-items: center;
      gap: 15px;
      padding: 20px 30px;
      width: 100%;
      background-color: #ffffff;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
    }

    #logo {
      height: 50px;
      border-radius: 8px;
    }

    #heading {
      font-size: 24px;
      font-weight: bold;
      color: #4a5fd3;
    }

    /* Navigation Bar */
    #nav {
      width: 100%;
      background-color: #4a5fd3;
      padding: 10px 0;
      text-align: center;
    }

    #nav ul {
      list-style: none;
    }

    #nav ul li {
      display: inline;
    }

    #nav ul li a {
      color: #fff;
      text-decoration: none;
      font-size: 16px;
      padding: 8px 20px;
    }

    #nav ul li a:hover {
      text-decoration: underline;
    }

    /* Login Box */
    #loginbox {
      background-color: #fff;
      margin-top: 60px;
      padding: 40px 30px;
      border-radius: 12px;
      width: 100%;
      max-width: 400px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
    }

    #loginbox h2 {
      margin-bottom: 20px;
      text-align: center;
      color: #2e3a59;
    }

    #loginbox span[style="color:red"] {
      display: block;
      text-align: center;
      margin-bottom: 10px;
      font-size: 14px;
    }

    /* Input Styling */
    #input[type="email"],
    #input[type="password"] {
      width: 100%;
      padding: 14px;
      margin: 10px 0 20px;
      font-size: 15px;
      border: 1px solid #ccc;
      border-radius: 8px;
    }

    /* Button Group */
    .button-group {
      display: flex;
      justify-content: space-between;
    }

    #inputbtn[type="submit"],
    #inputbtn[type="reset"] {
      width: 48%;
      padding: 12px;
      font-size: 15px;
      border: none;
      border-radius: 8px;
      background-color: #4a5fd3;
      color: #fff;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    #inputbtn[type="submit"]:hover,
    #inputbtn[type="reset"]:hover {
      background-color: #3a49b1;
    }

    @media (max-width: 480px) {
      #loginbox {
        padding: 30px 20px;
        margin-top: 40px;
      }

      #header {
        flex-direction: column;
        text-align: center;
        gap: 10px;
      }
    }
  </style>
</head>
<body>

  <div id="header">
    <img src="images/logo.jpg" id="logo" alt="Logo" />
    <span id="heading">RecruitEase</span>
  </div>

  <div id="nav">
    <ul>
      <li><a href="#">Home | Admin Panel</a></li>
    </ul>
  </div>

  <div id="loginbox">
    <h2>Admin Login</h2>
    <span style="color:red"><%= message %></span>

    <form action="/admin/adminLogin" method="post">
      <input id="input" type="email" placeholder="Enter Email" name="email" required />
      <input id="input" type="password" placeholder="Enter Password" name="password" required />

      <div class="button-group">
        <input id="inputbtn" type="submit" value="Login" />
        <input id="inputbtn" type="reset" value="Reset" />
      </div>
    </form>
  </div>
</body>
</html>
